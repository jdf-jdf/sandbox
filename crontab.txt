# THE TRIGGER -- "a trigger that is not you".
#
# Install with:   crontab crontab.txt
# Verify with:    crontab -l
# Watch it fire:  tail -f logs/cron.log
#
# Every weekday at 08:00. Change the path to wherever this repo lives.
0 8 * * 1-5 /ABSOLUTE/PATH/TO/repo/run.sh

# For the demo, prove it fires without waiting a day -- run every minute,
# let it fire twice, then comment it back out and commit logs/cron.log:
# * * * * * /ABSOLUTE/PATH/TO/repo/run.sh
